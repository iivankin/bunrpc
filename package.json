{
  "name": "brpc-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "check": "bun run check:core && bun run check:react && bun run typecheck:example",
    "check:core": "bun run --cwd packages/core check",
    "check:react": "bun run --cwd packages/react check",
    "typecheck": "bun run typecheck:core && bun run typecheck:react && bun run typecheck:example",
    "typecheck:core": "bun run --cwd packages/core typecheck",
    "typecheck:react": "bun run --cwd packages/react typecheck",
    "typecheck:example": "bun run --cwd apps/example typecheck",
    "test": "bun run --cwd packages/core test && bun run --cwd packages/react test"
  }
}
